{
  "name": "nodejs-artifacts-demo",
  "version": "1.0.0",
  "description": "Demo project for GitHub Actions artifacts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1" 
  },
  "dependencies": {
    "fs": "^0.0.1-security"
  }
}
